# Test: deliberate conflicts for MIGRATION_WARNING output
general {
    sensitivity = 0.5
    border_size = 2
}

general {
    sensitivity = 0.7  # conflict: same key in same section
}

windowrule = float, class:^(kitty)$
windowrule = tile, class:^(kitty)$     # conflict: same class, opposite rules

bind = SUPER, Return, exec, kitty
bind = SUPER, Return, exec, foot        # conflict: same bind, different target
