# Test: animations and layer rules
animatelist {
    animation = windows, 1, 10, myBezier, slide
    animation = fade, 1, 5, myBezier
    animation = workspaces, 0, 0
}

layerrule = blur, waybar
layerrule = blur off, firefox
layerrule = no_anim, selection

bezier = myBezier, 0.05, 0.9, 0.1, 1.05
