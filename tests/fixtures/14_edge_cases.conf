# Edge cases: ${name} syntax, empty monitor, scale=0
$mainMod = SUPER
$term = kitty

# ${name} variable syntax
bind = ${mainMod}, T, exec, ${term}

# Empty monitor
monitor = , preferred, auto, 1

# Scale=0 monitor
monitor = DP-1, 1920x1080@60, 0x0, 0
